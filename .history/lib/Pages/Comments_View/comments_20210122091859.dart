import 'package:flutter/material.dart';

class Comments extends StatelessWidget {
  @override
  Widget build(BuildContext context, postImage) {
    return Center(
      child: Hero(tag: 'image', child: child),
    );
  }
}
