import 'package:flutter/material.dart';

class ImageView extends StatelessWidget {
  final NetworkImage image;

  ImageView({Key key, this.image})
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
